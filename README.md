# WinBasic
![Windows 7 basic ~~cosplay~~ theme for icewm](screenshot.png)
## Installation
Just run these commands:
```
cd ~/.icewm/themes
git clone https://github.com/dibof228/winbasic
```
and through `Start menu` > `Settings` > `Themes` switch to this theme
